{
  "name": "@czarsimon/httpclient",
  "version": "0.5.0",
  "description": "Http client with reties based on axios",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "test": "jest",
    "lint": "echo \"Error: no linting rules specified\" && exit 0",
    "format": "echo \"Error: no formating specified\" && exit 0",
    "build": "tsc",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CzarSimon/httpclient.git"
  },
  "author": "Simon Lindgren",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/CzarSimon/httpclient/issues"
  },
  "homepage": "https://github.com/CzarSimon/httpclient#readme",
  "devDependencies": {
    "@types/jest": "^24.9.0",
    "@types/uuid": "^3.4.6",
    "jest": "^24.9.0",
    "prettier": "^1.18.2",
    "ts-jest": "^24.0.2",
    "tslint": "^5.18.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.7.4",
    "wait-for-expect": "^1.2.0"
  },
  "files": [
    "lib/**/*"
  ],
  "dependencies": {
    "@czarsimon/circutbreaker": "^1.0.0",
    "@czarsimon/remotelogger": "^0.9.0",
    "uuid": "^3.3.3"
  }
}
